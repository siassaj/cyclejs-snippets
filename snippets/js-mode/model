# -*- mode: snippet -*-
# Skeleton for model function
# name: cyclejs#model
# key: cyclemodel
# --
import xs from 'xstream'

function model(actions) {
  return xs.merge(
    xs.empty()
  )
}

export default model
