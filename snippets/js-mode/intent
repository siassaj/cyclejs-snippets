# -*- mode: snippet -*-
# Skeleton for intent function
# name: cyclejs#intent
# key: cycleintent
# --
import xs from 'xstream'

function intent(sources) {
  const DOM = sources.DOM

  return {}
}

export default intent