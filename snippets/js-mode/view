# -*- mode: snippet -*-
# Skeleton for view function
# name: cyclejs#view
# key: cycleview
# --
import xs      from 'xstream'
import { div } from '@cycle/dom'

function view(state$) {
  return xs.combine(...arguments).map(([state]) => {
    return div($0)
  })
}

export default view
