# -*- mode: snippet -*-
# name: cyclejs#operation
# key: cycleoperation
# --
import xs from 'xstream'

function $1(actions) {

  ${2:const $3 = $0}

  return xs.merge(
    {$3}
  )
}

export default {$1}
