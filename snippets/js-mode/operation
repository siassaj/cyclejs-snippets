# -*- mode: snippet -*-
# Skeleton for generic operation functions
# name: cyclejs#operation
# key: cycleoperation
# --
import xs from 'xstream'

function $1(actions) {

  return xs.merge(
    xs.empty()
  )
}

export default {$1}
